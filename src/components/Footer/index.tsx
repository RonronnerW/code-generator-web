import '@umijs/max';
import React from 'react';

const Footer: React.FC = () => {
  const defaultMessage = 'RonronnerW';
  const currentYear = new Date().getFullYear();
  return (
    <></>
  );
};
export default Footer;
